# Obsidian Knowledge Map Configuration
# Copy this file to .env and update the values for your setup

# Required: Path to your Obsidian vault
VAULT_PATH=/path/to/your/obsidian/vault

# Optional: Server configuration
SERVER_PORT=7001
HOST=0.0.0.0

# Optional: SSL configuration (for HTTPS)
# SSL_CERT=/path/to/cert.pem
# SSL_KEY=/path/to/key.pem
# SSL_PASSWORD=your_ssl_password

# Optional: Debug mode
# DEBUG=false

# Note: LLM and processing configuration is now handled by llm_config.yaml
# Use 'uv run python cli/manage_llm_config.py status' to view current settings
# Copy llm_config_example.yaml to llm_config.yaml and customize for your setup

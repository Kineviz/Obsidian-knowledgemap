# Environment variables for Knowledge Map Tool
# Copy this file to .env and fill in your actual values

# OpenAI API Key (required for entity extraction)
# Used by: step1_extract.py, manual_trigger.py, step4_monitor.py, main.py
OPENAI_API_KEY=your_openai_api_key_here

# Docker Compose Configuration
# These variables are used by docker-compose.yml for container configuration
# ideally, use full path, like
# VAULT_PATH=Users/your_user_name/Obsidian/ExampleVault
VAULT_PATH=~/Obsidian/ExampleVault
SERVER_PORT=7001
MAX_CONCURRENT=5

# Note: For local development, use command-line arguments:
# uv run step4_monitor.py --vault-path '/path/to/vault' --server-port 7001
